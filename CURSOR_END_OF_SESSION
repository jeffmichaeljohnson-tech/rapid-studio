#!/bin/bash
# CURSOR END OF SESSION - Shared Context System for Rapid Studio
# Run this command when ending your session with Cursor
# Writes to .ai-context/ for both Cursor and Claude synchronization

echo "💻 CURSOR END OF SESSION - Preserving context for both Cursor and Claude..."

# Change to project directory
cd "$(dirname "$0")"

# Run the Cursor shared context end session script
python tools/cursor_end_session.py

echo ""
echo "✅ Cursor session context preserved for both agents!"
echo "🔄 Claude can now see what Cursor implemented"
echo "🔄 Cursor can now see what Claude discussed"
echo "🎯 Perfect synchronization achieved!"
